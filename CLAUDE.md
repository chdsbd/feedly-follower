# foodieyak-follower

## Overview

This app is a Safari Web Extension built with web technologies.

The directory Shared (Extension)/Resources contains the majority of the logic.

## Browser Extension Notes

- This is a browser extension, not a regular web app. Always use the WebExtension APIs (`browser.*`) for extension operations like accessing extension resources, storage, and messaging.
